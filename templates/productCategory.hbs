---
{
    "stream": {
        "$id": "categories",
        "source": "knowledgeGraph",
        "destination": "pages",
      "filter": {
        "entityTypes": [
          "ce_productCategory"
        ],
        "savedFilterIds": [
          "822661827"
        ]
      },
      "fields": [
        "id",
        "uid",
        "meta",
        "name",
        "slug",
        "c_image",
        "richTextDescription",
        "c_relatedProducts.name",
        "c_relatedProducts.slug",
        "c_relatedProducts.photoGallery",
        "c_relatedProducts.brand",
        "c_relatedProducts.price",
        "c_relatedProducts.color",
        "c_relatedProducts.richTextDescription"
      ],
      "localization": {
          "locales": ["en"]
      }
    },
    "urlFormat": "{{slug}}"
}
---
<!DOCTYPE html>
<html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <meta http-equiv="X-UA-Compatible" content="ie=edge">
      <title>{{name}}</title>
      <meta name="description" content="{{description}}">
      {{>head}}
      <link rel="stylesheet" href="/css/font-awesome.min.css" />
      <link rel="stylesheet" href="/fonts/fonts.css" />
    </head>
    <body class="bg-white text-gray-600 font-Poppins leading-normal text-base tracking-normal">
      {{>top-nav}}
      {{>navbar}}
      <div class="w-full max-w-7xl mx-auto py-8 px-6 text-center">
        <div class="w-full text-center mb-10 relative h-24">
            <img class="w-full h-24 object-cover" src="{{c_image}}" />
            <div class="w-full absolute top-0 bottom-0 left-0 right-0 text-6xl align-middle bg-red-400 bg-opacity-25 py-5 text-textblack font-bold">{{name}}</div>
        </div>
        <div class="mb-6 text-greytext inline-block w-full text-base">
            <p>{{convertRTFToHTML richTextDescription}}</p>
        </div>
      </div>
      <div class="flex flex-wrap w-full max-w-7xl mx-auto">
        <div class="flex flex-wrap">
          {{#each c_relatedProducts}}
            <div class="w-full sm:w-1/2 md:w-1/2 lg:w-1/3 xl:w-1/4 px-4 text-center mb-10">
              <div class="mb-5 w-full">
                {{#each photoGallery}}
                    {{#if @first}}
                        <a class="w-full inline-block border border-black border-opacity-10" href={{slug}}>
                            <img class="hover:grow hover:shadow-lg object-contain aspect-square" src={{image.url}}>
                        </a>            
                    {{/if}}
                {{/each}}
              </div>
              <h3 class="mb-3 sm:h-12"><a class="tracking-wide no-underline hover:no-underline font-normal text-gray-800 text-base mb-8 hover:text-primary" href={{slug}}>{{name}}</a></h3>
              <div class="font-semibold text-xl">${{price.value}} </div>
            </div>
          {{/each}}
        </div>
      </div>
      {{>footer}}
    </body>
</html>