---
{
    "stream": {
        "$id": "products",
        "source": "knowledgeGraph",
        "destination": "pages",
      "filter": {
        "entityTypes": [
          "product"
        ]
      },
      "fields": [
        "id",
        "uid",
        "meta",
        "richTextDescription",
        "name",
        "slug",
        "price",
        "color",
        "brand",
        "photoGallery"
      ],
      "localization": {
          "locales": ["en"]
      }
    },
    "urlFormat": "{{slug}}"
}
---
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>{{name}}</title>
        <meta name="description" content="{{description}}">
        {{>head}}
    </head>
    <body class="bg-white text-gray-600 font-Poppins leading-normal text-base tracking-normal">
        {{>top-nav}}
        {{>navbar}}
            <div class="flex flex-wrap w-full max-w-7xl mx-auto py-8 lg:py-20">
                <div class="w-full md:w-1/2">
                    <div class="w-full md:w-full p-6 flex">
                        {{>photoGallerySingle photoGallery}}
                    </div>
                </div>
                <div class="w-full md:w-1/2">
                    <div class="md:py-8 px-6 mx-auto">
                        <div class="tracking-wide no-underline hover:no-underline font-bold text-gray-800 text-3xl md:text-4xl mb-6">
                            {{name}} 
                        </div>
                        <div class="tracking-wide no-underline hover:no-underline font-bold text-gray-600 text-xl mb-6">
                            {{brand}} 
                        </div>
                        <div class="tracking-wide no-underline hover:no-underline font-bold text-primary text-3xl mb-6">
                            ${{price.value}} 
                        </div>
                        <div class="mb-6 text-greytext inline-block w-full text-base">
                            <p>{{convertRTFToHTML richTextDescription}}</p>
                        </div>
                        <p class="text-base"><b>Color:</b> {{color}}</p>
                    </div>
                </div>
            </div>
        {{>footer}}
    </body>
</html>